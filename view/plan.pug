.signup-child
  .signup-header
  -console.log(plan)
    h3 PREMIUM
    .signup-pricing
      span.pricing $#{plan.price}
      | /month
      p That’s only 13.30$ per meal
  .signup-features
    ul
      li
        i.fas.fa-check.small-icon  
        | 1 meal every day
      li
        i.fas.fa-check.small-icon  
        | Rated #{plan.averagerating}
      li
        i.fas.fa-check.small-icon  
        | Access to newest creations
      li
        i.fas.fa-check.small-icon  
        | Free delivery
    .signup-button
      a.btn.btn-full(plan-id = `${plan["_id"]}`) i am hungary